#include "../header-vmdl/externc.vmdh"

(vmInstruction, needContext, tailCall)
nextpropnameidx : JSValue -> JSValue

nextpropnameidx (obj) {
    //NOTE! Wrap function
    JSValue wrapped_return_value = JS_EMPTY;
    Nextpropnameidx(obj);
    return wrapped_return_value;
}