\inst fixnum (Register dst, Immediate num)

\otherwise \{
/*
  fixnum dst imm
  dst : destination register
  imm : immediate value of the fixnum
  $dst = imm
 */

  dst = small_cint_to_fixnum((cint) num);
\}
